image: node:alpine

pages:
  script:
  - apk --no-cache add py2-pip python-dev python libc6-compat build-base automake autoconf
  - pip install sphinx
  - mkdir -p cert && touch cert/server.key && touch cert/server.crt && touch cert/ca.crt
  - make html SPHINXOPTS=-Ea
  - mv build/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
