image: node:alpine

pages:
  script:
  - apk --no-cache add py2-pip python-dev libc6-compat
  - pip install sphinx
  - mkdir -p cert && touch cert/server.key && touch cert/server.crt && touch cert/ca.crt
  - npm install
  - make html SPHINXOPTS=-Ea
  - grunt build
  - mv build/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
